x = float(input('x = '))
y = float(input('y = '))

# Найти среднее арифметическое:
average = (x + y) / 2
print('Average = ', average)

# Среднее геометрическое:
geom = (x * y) ** (1 / 2)
print('Geomrtric = ',geom)
